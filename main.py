import User
import threading
import random

def main():
    users = []
    for i in range(200):
        id = i
        tipo = random.uniform(0, 5)
        saldo = random.uniform(10, 100)
        usuario = 
